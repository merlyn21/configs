---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-env-secret
type: Opaque
data:
  {{- range $key, $val := index .Values "env-secret" }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
